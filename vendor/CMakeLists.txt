# if(MSVC AND NOT "${MSVC_VERSION}" LESS 1400)
# 	add_definitions( "/MP" )
# endif()


### RakNet ###
# RakNet already has a CMakeLists.txt so let's use these

# set(RAKNET_VERSION 4.081)
# include_directories(
# 	RakNet
# )

# add_subdirectory( RakNet )
